import max_profit as maxpro
import prime_number as primenum

if __name__ == '__main__':
    primenum.is_prime_number(1)
    primenum.is_prime_number(13)
    primenum.is_prime_number(24)
    primenum.is_prime_number(31)
    